<div class="container-x py-16">
  <h1 class="text-3xl md:text-5xl font-black tracking-tight mb-6">{{ page.title }}</h1>
  <div class="prose max-w-none text-slate-700">
    {{ page.content }}
  </div>
</div>
